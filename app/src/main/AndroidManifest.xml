<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- Android 10 (API 29) 及以上版本，可能需要添加 -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 前台服务类型,对应 SmbDownloadService / DownloadService 的 dataSync -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- WiFi 传输功能读取连接信息(WiFiClientActivity) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--
      play-services-measurement-api(经 firebase-analytics 传递引入)会注入这两个广告 ID 权限。
      本应用不做广告也不做归因,而且仓库里没有 google-services.json,Firebase 插件根本不会启用,
      analytics 实际是惰性的 —— 却仍会让侧载安装界面出现"广告 ID"字样。合并阶段移除。
    -->
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />
    <!-- 同源:广告 ID 本体,以及 Play 安装来源归因服务的绑定权限。都不需要。 -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE"
        tools:node="remove" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" /> <!-- 鼠标适配 -->
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <application
        android:name=".EhApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_scheme"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        tools:ignore="UnusedAttribute">
        <activity
            android:name=".ui.TagSelectorActivity"
            android:label="标签选择"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            xmlns:tools="http://schemas.android.com/tools"
            tools:replace="android:theme" />
        <!-- tools:targetApi="n"> -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

        <activity
            android:name=".ui.splash.SplashActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="orientation|keyboardHidden|screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="fullSensor"
            android:theme="@style/SplashTheme"
            android:windowSoftInputMode="stateHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <!-- LAUNCHER -->
                <!-- <category android:name="android.intent.category.HOME" /> -->
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.Main"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- <category android:name="android.intent.category.LAUNCHER"/> -->
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="exhentai.org" />
                <data android:host="e-hentai.org" />
                <data android:host="g.e-hentai.org" />
                <data android:host="lofi.e-hentai.org" />
            </intent-filter>
            <intent-filter android:label="@string/keyword_search">
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter android:label="@string/image_search">
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.GalleryActivity"
            android:configChanges="screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.Gallery">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/7z" />
                <data android:mimeType="application/rar" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-7z-compressed" />
                <data android:mimeType="application/x-rar-compressed" />
                <data android:mimeType="application/x-zip-compressed" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.7z" />
                <data android:pathPattern=".*\\.rar" />
                <data android:pathPattern=".*\\.zip" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.7z" />
                <data android:pathPattern=".*\\.rar" />
                <data android:pathPattern=".*\\.zip" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.samsung.android.support.REMOTE_ACTION" />
            </intent-filter>

            <meta-data
                android:name="com.samsung.android.support.REMOTE_ACTION"
                android:resource="@xml/s_pen_actions" />
        </activity>
        <activity
            android:name=".ui.SettingsActivity"
            android:configChanges="screenSize|uiMode"
            android:theme="@style/AppTheme.Settings" />
        <activity
            android:name=".ui.ExcludedLanguagesActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/excluded_languages"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.DirPickerActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/dir_picker"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.LicenseActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/license"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.FilterActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/filter"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.BlackListActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/blacklist"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.SetSecurityActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/set_pattern_protection"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.UConfigActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/u_config"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.MyTagsActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/my_tags"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.HostsActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/hosts"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".shortcuts.ShortcutsActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name=".ui.wifi.WiFiServerActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/wifi_server"
            android:theme="@style/AppTheme.Toolbar" />
        <activity
            android:name=".ui.wifi.WiFiClientActivity"
            android:configChanges="screenSize|uiMode"
            android:label="@string/wifi_client"
            android:theme="@style/AppTheme.Toolbar" />

        <service
            android:name=".download.DownloadService"
            android:foregroundServiceType="dataSync"
            android:label="@string/download_service_label" />

        <service
            android:name=".smb.SmbDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="com.hippo.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="com.hippo.content.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>
    </application>

</manifest>